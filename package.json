{
  "name": "eliot-research",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.23.0",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "build:pwa": "pnpm --filter @eliotr/pwa build",
    "build:worker": "pnpm --filter @eliotr/core build",
    "typecheck": "tsc -b --pretty false",
    "lint": "eslint .",
    "test": "pnpm test:provisioners && vitest run && pnpm --filter @eliotr/core test",
    "test:watch": "vitest",
    "contracts:check": "node scripts/check-contract-fixtures.mjs",
    "boundaries:check": "node scripts/check-boundaries.mjs",
    "boundaries:negative": "node scripts/test-boundary-negative.mjs",
    "budgets:check": "node scripts/check-budgets.mjs",
    "delivery:check": "node scripts/check-delivery-authority.mjs",
    "ingest:check": "node scripts/check-ingest-admission.mjs",
    "projection:check": "node scripts/check-projection-execution.mjs",
    "gemini:check": "node scripts/check-gemini-integration.mjs",
    "branch-hygiene:check": "node scripts/test-branch-hygiene.mjs",
    "rust:boundaries": "node scripts/check-rust-boundaries.mjs",
    "rust:vectors": "node scripts/check-rust-vectors.mjs",
    "rust:fmt": "cargo fmt --all --check",
    "rust:clippy": "cargo clippy --workspace --all-targets --all-features --locked -- -D warnings",
    "rust:test": "cargo nextest run --workspace --all-features --locked && cargo test --doc --workspace --all-features --locked",
    "rust:deny": "cargo deny check",
    "rust:wasm": "cargo build --workspace --target wasm32-unknown-unknown --release --locked && node scripts/check-rust-wasm.mjs --mode default && cargo build --package eliotr-kernel-wasm --target wasm32-unknown-unknown --release --features m1-self-test-export --locked && node scripts/check-rust-wasm.mjs --mode self-test",
    "rust:coverage": "cargo +nightly-2026-08-31 llvm-cov --package eliotr-canonical --package eliotr-test-vectors --all-features --locked --branch --fail-under-lines 90 --text",
    "rust:check": "pnpm rust:boundaries && pnpm rust:vectors && pnpm rust:fmt && pnpm rust:clippy && pnpm rust:test && pnpm rust:deny && pnpm rust:wasm && pnpm rust:coverage",
    "check": "pnpm contracts:check && pnpm boundaries:check && pnpm boundaries:negative && pnpm budgets:check && pnpm work-packets:check && pnpm branch-hygiene:check && pnpm delivery:check && pnpm ingest:check && pnpm projection:check && pnpm evidence:check && pnpm erasure:check && pnpm gemini:check && pnpm lint && pnpm typecheck && pnpm test && pnpm check:implementation-status && pnpm rust:check",
    "check:affected": "pnpm contracts:check && pnpm boundaries:check && pnpm boundaries:negative && pnpm budgets:check && pnpm work-packets:check && pnpm branch-hygiene:check && pnpm delivery:check && pnpm ingest:check && pnpm projection:check && pnpm evidence:check && pnpm erasure:check && pnpm gemini:check && pnpm lint && pnpm typecheck && pnpm test && pnpm check:implementation-status && pnpm rust:check",
    "work-packets:check": "node scripts/check-work-packets.mjs",
    "cf:types": "pnpm --filter @eliotr/core cf:types",
    "cf:dev": "node scripts/local-runtime.mjs --dev",
    "cf:dry-run": "pnpm build:pwa && pnpm --filter @eliotr/core deploy:dry-run",
    "cf:preflight:remote": "node scripts/provision-cloudflare-core.mjs --check-only && node scripts/provision-ai-search.mjs --check-only && node scripts/provision-ai-gateways.mjs --check-only && node scripts/provision-cloudflare-access.mjs --check-only",
    "cf:provision:core": "node scripts/provision-cloudflare-core.mjs",
    "cf:provision:search": "node scripts/provision-ai-search.mjs",
    "cf:provision:gateways": "node scripts/provision-ai-gateways.mjs",
    "cf:provision:access": "node scripts/provision-cloudflare-access.mjs",
    "cf:deploy": "node scripts/deploy-cloudflare.mjs",
    "test:provisioners": "node scripts/test-access-runtime-config.mjs && node scripts/test-access-runtime-preflight.mjs && node scripts/test-foundation-receipt-schema.mjs && node scripts/run-provisioner-tests.mjs",
    "check:implementation-status": "node scripts/check-implementation-status.mjs",
    "evidence:check": "node scripts/check-evidence-resolution.mjs",
    "erasure:check": "node scripts/check-erasure-closure.mjs",
    "local:prepare": "node scripts/local-runtime.mjs --prepare",
    "local:dev": "node scripts/local-runtime.mjs --dev",
    "launch:code": "node scripts/check-launch-code.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "1.1.0",
    "@cloudflare/workers-types": "5.20260827.1",
    "@eslint/js": "10.0.1",
    "@types/node": "26.4.0",
    "eslint": "10.9.1",
    "typescript": "6.0.3",
    "typescript-eslint": "8.68.0",
    "vite": "8.2.2",
    "vitest": "4.1.10",
    "wrangler": "4.127.1"
  }
}
