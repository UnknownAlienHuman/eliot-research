# Agents ER-04 through ER-07 — retrieval

Claim exactly one packet: planner, projection, managed/D1 lanes, or evidence resolution. Index results are
locators/previews, never evidence. `VERIFY` reopens exact admitted revision; `EXHAUSTIVE` never depends on
top-k/reranking.
