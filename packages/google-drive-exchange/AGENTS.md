# Agents ER-18 through ER-20 — Google Drive Exchange

Package is an expiring transport experiment, not a second backend. Claim one: serializer, cursor reconciler,
or OAuth/result publication. Rows are UNTRUSTED/CANDIDATE_ONLY. Correctness requires cursor replay, R2 freeze,
exact IDs/hashes, readback.
