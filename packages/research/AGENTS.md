# Agents ER-08 through ER-12 — research

Claim one packet only: investigation ledger, workflow, claim audit/coverage, artifact compiler, or Wiki.
Chat is a view over an Investigation. Every expensive call checkpoints first and returns handles, not
large text. Evidence discovered after freeze requires an explicit reopen and a new freeze revision.
