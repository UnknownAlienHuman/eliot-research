# Agents ER-21 and ER-22 — interfaces

Own semantic HTTP/federation surface, not application behavior. Clients choose products/scopes, never
D1/R2/index/provider primitives. Large results use stable handles, ranges, manifests, hashes, cursors.
Error responses are typed and never leak source text/secrets.
