# Agents ER-21 and ER-22 — interfaces

Own the semantic HTTP/federation surface, not application behavior. Clients choose products and scopes,
never D1/R2/index/provider primitives. Large results use stable handles, ranges, manifests, hashes, and
cursors. Error responses are typed and never leak source text or secrets.
