# Implementation gap register

This register prevents agents from confusing broad architecture coverage with executable coverage.
It supplements the per-agent packets; it does not create a second ownership system. The dependency order,
production definition and final exit evidence are specified in
[`production-readiness-plan.md`](production-readiness-plan.md).

| Priority | Gap | Existing owner | Closure evidence |
|---|---|---|---|
| P1 | Local signed owner bridge, explicit read-policy command and guarded initial immutable-import namespace setup are implemented; real Access login qualification and complete populated owner loops remain open | ER-44, ER-43, ER-24, ER-25, ER-26 | signed-verifier, loopback session/CSRF/logout and policy CAS tests; retain actual owner login and finish full source-to-evidence loops; see `local-launch.md` |
| P1 | Deployment HTTP verification and release ordering are implemented but not live-qualified; complete binding/version attestation is still missing | ER-26, ER-27 | retain real generation-bound authenticated smoke, full binding/version readback and failure/redeploy receipts; see `deployment-audit-2026-09-04.md` |
| P1 | Pinned normalized byte/line excerpt materialization is implemented with key/version/ETag/size/checksum pinning (`content-store.ts`, 14 controlled-R2 cases); retrieval Q2 exact verification over bounded pinned bytes is implemented (`evidence-resolver.ts` `verifyPinnedExactEvidence`, 11 pure cases incl. mid-resolution byte-mutation refusal, Unicode/table-cell/old-revision/corrupt-map/range-mismatch/forged-handle/revocation); Q3 query orchestration core is implemented over injected ports (`service.ts` `createRetrievalQueryService`, 13 pure cases incl. freeze-before-lanes, direct-before-semantic, no-hit≠absence, retry-vs-changed, budget/cancel, scope-stale, trace-substitution, UNION/EXCEPT binding, family diversity) and D1 query-result/trace persistence is implemented (`0021_retrieval_query_persistence.sql` additive tables plus `query-persistence.ts` D1 scope/result/trace/budget ports, 6 node:sqlite cases over the committed stream incl. persist+readback, replay-vs-conflict, expired/denied with zero rows, substituted-trace refusal, grant-revoke invalidation); Worker `research.query` composition is implemented at the app layer (`research-session.ts` over D1 scope/result/trace ports, IDENT+LEX D1 Search lanes, RRF fusion, exact D1+R2 resolver, budget guard, `0022` profile binding with replay-before-freeze, 6 real-D1/R2 cases over the Q1 pipeline); per-candidate R2 byte resolution fixed by the disclosed ER-07 narrow #135 fix (`content-store.ts:136` now sends unquoted `etag`; first success proven: a real LEX hit resolves 28 pinned R2 bytes to a COMPLETE/SAMPLED result+trace with duplicate-free replay); research.trace now serves persisted query-* traces owner-bound over D1 (trace-read.ts read-only SELECTs over 0021 tables, 4 real-D1/R2 cases: LEX-hit SAMPLED readback, orient unchanged, foreign==missing 404, malformed 400/unknown 404 with zero writes); SEM lane executor is implemented over the managed-search surface with pinned-generation translation and honest degradation, live AI Search readback/promotion receipts remaining open; sharded exhaustive plan/execute/merge is implemented (`exhaustive.ts` pure planner/executor/merger reusing `verifyPinnedExactEvidence` with no second verifier: scope-derived denominator, hard-bound splits, earned COMPLETE-only-on-full-settlement merge with timeout/cancel kept unknown, 14 `exhaustive.test.ts` cases incl. a real-D1 scope freeze plus 8 `exhaustive-negatives.test.ts` cases incl. Unicode/table-cell/old-revision/corrupt-map/range-mismatch/forged-handle/revocation; RETRIEVAL slice stays disabled and the Q3 D1 result store still caps persisted coverage at SAMPLED, so a COMPLETE persistence path through the Q3 store stays refused) and the Q7 loop is closed over additive `0023` (see Closure evidence) | ER-04, ER-07 | ER-07 exact resolver plus sharded scan planner/merger with Unicode/table-cell/old-revision/corrupt-map/range-mismatch/forged-handle/revocation negatives; `content-store.test.ts` 14 pass retained; `evidence-resolver.test.ts` 11 pass; `retrieval.test.ts` 14 pass; `query-persistence.test.ts` 6 pass; `research-query-retrieval.test.ts` 6 pass; `research-trace-read.test.ts` 4 pass; SEM lane executor is implemented over the managed-search surface with pinned-generation translation and honest degradation (`lanes.ts` `createSemLaneExecutor`: plan-derived expansion, NOT_PROMOTED degrades to SKIPPED, mid-read registry rotation fails closed, local proof_state marker dropped at the lane boundary; `research-session.ts` composes SEM via the D1-backed managed port only when a namespace binding plus a promoted registry generation exist; 3 real-D1/R2 cases prove SEM in lanes_used with a semantically-found candidate resolved to pinned R2 bytes at SAMPLED, rotation caught with zero resolutions, and unpromoted degradation without provider contact; coverage stays SAMPLED with no exhaustive denominator); `lanes.test.ts` 5 pass; `research-query-sem.test.ts` 3 pass; fix or replace the quoted-etag conditional read with a runtime-accepted pin before claiming resolved evidence through `research.query`; RetrievalRequest carries only evaluated inputs after the contract amendment (`policy` removed from `packages/retrieval/src/ports.ts` with zero `packages/contracts` edits: the field had zero readers, every guarantee it implied is already enforced by freeze/grant/currentness plus per-candidate evidence resolution, and a second gate over the same D1 rows would drift; `retrieval.test.ts` pins the exact key set with unchanged behaviour; Q7 ordered reconcile loop persists an earned COMPLETE with its denominator over additive `0023` (`exhaustive-reconcile.ts`, 7 real-D1 cases: earned persist+readback, replay without re-execution/duplicates, partial resume without double-count, unsettled UNFINISHED with zero result rows, foreign-denominator and denominator-less refusals, partial-finalize refusal; the ORIENT profile of `research.query` remains unchanged, RETRIEVAL slice stays disabled; Q8 now composes `EXHAUSTIVE_JOB` behind the existing route through a versioned typed result and separate parser; the default path now validates admitted normalized manifests, persisted structural projection ranges, creates exact handles through the canonical evidence authority and reads pinned R2 bytes before the Q7 D1 loop; the ER09 ResearchWorkflow Q8 host extension and local HTTP Workflow launch/readback/cancel composition are implemented, while deployed Workflow, RETRIEVAL enablement and live qualification remain open) |
| P1 | Generic federation boundary is implemented but not wired into the Worker composition root or live-qualified | ER-22, ER-24 | compose authenticated D1/R2 authority ports, expose routes, and retain mutual-auth/idempotency/range/cursor receipts |
| P1 | Rust M1 foundation and narrow M2 shadow primitives exist; complete family parity and M3–M7 authority promotion remain open | ER-00, ER-01, ER-02, ER-03 and capability owners | audit/reuse existing safe-integer canonical JSON, SHA-256, stable IDs and residency shadow code; complete uncovered M2 identity/serialization parity; M3–M4 deterministic-domain migration; M5 Wasm/differential shadow, M6 per-family Rust authority promotion and M7 superseded TypeScript removal |
| P1 | Exact erasure closure is implemented but not live-qualified across Cloudflare/provider/offsite paths | ER-28, ER-34 | remote R2/D1/AI Search/offsite deletion, blocked-lock, restart replay and restore purge-ledger receipts |
| P1 | Projection generations and readiness execution are implemented but not live-qualified | ER-05, ER-06, ER-15, ER-16, ER-24, ER-38 | remote pinned-R2 readback, D1 Search shadow activation/rollback, AI Search item readback, promoted managed generation and Queue replay receipts |
| P1 | Exact EvidenceHandle resolution and citation/output gating are implemented but not live-qualified | ER-07, ER-11, ER-13, ER-19, ER-21, ER-24, ER-39 | deployed Access grant, remote D1 Core/Search guard, checksum-bound R2 range readback, purge invalidation and end-to-end citation receipt |
| P1 | Raw-file fast cloud ingest (§4.1) is not composed; normalized-folder upload alone does not satisfy it | ER-05, ER-14, ER-29, ER-24, ER-25 | implement bounded managed conversion, qualification and projection adapters with local recorded-provider failure tests and full browser/storage loop; no in-Worker document engine |
| P1 | Governed bundle ingest includes current-policy continuation and transaction-time revocation guards; remote qualification remains open | ER-13, ER-14, ER-21, ER-24, ER-29, ER-37 | deployed owner/service prepare, real multipart R2 readback/promotion, remote guarded D1 commit, duplicate/lost-ACK and Queue projection receipts |
| P1 | Worker composition and Access dispatch are implemented but not live-qualified | ER-17, ER-21, ER-24, ER-26 | deployed owner JWT, service-token class denial/allow fixtures and remote D1 catalog readback |
| P1 | The selected `GOOGLE_EXTERNAL_TRANSPORT=gemini-mcp` Workspace profile has candidate-only status/planning/receipt validation but no authenticated ELIOT candidate admission or canonical readback qualification; catalog is withheld until explicit service-scope read authority exists. gcloud/Google Cloud and Google AI Studio/Gemini API remain optional future profiles | ER-17, ER-18, ER-20, ER-24, ER-26, ER-36 | validated profile selection; deployed `gemini-spark` Access initialize/tools/list/tools/call, authenticated Workspace action/readback and a separate ELIOT admission/reconciliation receipt; qualify gcloud separately only when selected |
| P1 | D1 outbox, Queue inbox and projection acceptance are implemented but not live-qualified | ER-13, ER-15, ER-24 | remote Queue lost-ACK, duplicate delivery, poison message, DLQ and restart receipts |
| P1 | W2 monotone bounded executor and executable ResearchWorkflow binding run over D1/R2 with restart resume, single-effect replay and 64KiB handle-only steps; RESEARCH session/query/run composition is now executable over DO storage plus W1/W2 with the same negatives; governed model/evidence stages, branch scheduler and notification/retention/restore composition remain incomplete | ER-09, ER-10, ER-13, ER-16, ER-24 | W2 local real-storage monotone/restart/duplicate/cancel/lost-ACK/binding fixtures plus research-session.test.ts DO/HTTP replay/cancel/lost-ACK/64KiB fixtures; complete W3 settlement, W4 evidence/audit, branch scheduler, WebSocket transport and runtime closures before live qualification; see `workflow-checkpoints.md` |
| P1 | Managed search response needs strict locator decoding before canonical resolution | ER-06, ER-07, ER-16 | oversized/malformed/fake-handle fixtures; exact resolver required |
| P1 | Scope authority and explicit read grants are composed for owner metadata orientation and bounded FAST_SEARCH; full research/federation integration and live qualification remain open | ER-10, ER-24, ER-30 | retain remote policy/grant/currentness receipts and complete non-metadata product paths |
| P1 | Owner metadata Corpus Lens, normalized N1 structural maps, and the admitted table-cell native coordinate-map adapter are locally executable; native page/region/code precision, full structural expansion, Atlas and source-span evidence navigation remain incomplete | ER-06, ER-07, ER-24, ER-31, ER-39 | retain the Q1 import→R2/D1 normalized and table-cell map readbacks; complete exact source-span/Atlas paths, full query retrieval and retained remote receipts; see `local-launch.md` |
| P1 | Minimum Wiki, Artifact Compiler, trace and change products are not composed | ER-11, ER-12, ER-21, ER-24 | immutable revision/head CAS, fully resolved citations, copy-on-write update and purge dependency invalidation |
| P1 (unselected profile) | The separate server-owned `drive-exchange` profile remains incomplete: bounded Sheet/changes REST, contribution guards, encrypted D1 credential refresh/rotation, internal first-connection OAuth admission and owner-only OAuth begin/callback HTTP/PWA exist. Provisioning, Docs/export/publication and runtime composition remain missing. Durable leased cursor polling with ID/hash dedup and frozen-envelope tamper audit exists (`reconciler.test.ts`, 10 real-D1 cases). This legacy path is not a prerequisite for the selected `gemini-mcp` Workspace profile and must not trigger Google Cloud or custom OAuth setup | ER-18, ER-19, ER-20, ER-24, ER-26 | retain the unfinished Drive Exchange work for explicit future profile selection; the active Spark/Antigravity candidate-admission and readback requirements are tracked separately above; `drive-rest.md`, `drive-credentials.md`, `drive-oauth-admission.md`, `canonical-alignment.md` |
| P2 | Local preparation, signed Access browser bridge and explicit read-policy setup are implemented; live owner login is not yet qualified | ER-00, ER-26 | real owner/IdP session and current namespace-policy receipt, no auth bypass; `local-launch.md` |
| P2 | Backup/restore export exists only as design contour | ER-34 | clean-account restore with purge ledger applied before payload exposure |
| P2 | PWA has authorized Library-to-Lens, same-tab/reload recovery and read-only exact-folder discovery of lost operation IDs; authorized revision history and explicitly recorded-only channel states are connected; active-index readiness assessment, project editing, remaining failure UI and the full setup-to-import browser/storage loop remain incomplete (#98) | ER-25 | browser session/reconnect and full owner-loop tests with degraded dependencies |
| P2 | T2/T3 deterministic corpus is expanded with adjudicated RU/EN/code/table cases and collapsing-extractor negatives; live generation promotion and workload qualification remain open | ER-23, ER-31, ER-32 | GC-009..GC-012 adjudicated RU/EN/code/table cases, LF SHA-256 pins, promotion thresholds, collapsing/malformed/empty/oversized negatives; live T2/T3 quality and T6 receipts NOT_EXECUTED |
| P2 | T6 workload profile has not been measured | ER-35 | 5/20/50 readers, D1 contention, index throughput, cost and p95 receipts |

Rules:

1. Close the P0 authority and evidence path before adding advanced research products.
2. A live product gate cannot be replaced by a mock, typecheck, local Wasm run or dry-run.
3. A provider result remains a locator or candidate until the canonical readback path succeeds.
4. Update this table and `implementation-status.json` in the same change that removes or adds a scaffold.
5. `IMPLEMENTED_NOT_LIVE` means code and deterministic negative tests exist; it does not mean a platform round trip occurred.
6. `ACCEPTED`, Queue `ack()`, Workflow completion and research completion are independent states.
7. Google Workspace/gcloud success remains an untrusted transport observation until exact readback and ELIOT authority reconciliation.
8. Ingest `ADMITTED` requires a durable SourceRevision, exact promotion readback and projection outbox in the same guarded authority path.
9. An index hit or provider citation remains a locator until exact authorized R2 bytes produce a durable EvidenceHandle and resolution receipt.
10. AI Search `uploadAndPoll` completion remains shadow state until item readback and managed-generation promotion both succeed.
11. TypeScript and Rust may coexist during differential shadow migration, but permanent dual authority is prohibited.
12. SourceCard, DocumentMap, ProjectAtlas, reading routes and unresolved EvidenceHandle candidates are navigation-only and cannot satisfy publication support.
13. Production readiness requires the ordered exit evidence in `production-readiness-plan.md`, not merely an empty P0 list.
