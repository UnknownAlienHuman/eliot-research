# Agent ER-25 — PWA shell

The PWA has no authority and no direct Cloudflare/Google binding. It calls only the owner API. Keep
three persistent panels—corpus, investigation, evidence—and always expose health, freshness,
coverage, policy denial, budget stop, and connector degradation.
