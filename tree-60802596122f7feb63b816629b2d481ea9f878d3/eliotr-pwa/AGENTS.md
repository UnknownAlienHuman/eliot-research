# Agent ER-25 — PWA shell

PWA has no authority/direct Cloudflare/Google binding; call only the owner API. Keep three persistent
panels—corpus, investigation, evidence; always expose health, freshness, coverage, policy denial, budget
stop, and connector degradation.
