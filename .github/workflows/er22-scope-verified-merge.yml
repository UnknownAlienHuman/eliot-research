name: er22-scope-verified-merge

on:
  push:
    branches:
      - agent/ER-22-scope-depth-preflight-20260904

permissions:
  contents: write
  pull-requests: write
  checks: read
  actions: read

concurrency:
  group: er22-scope-verified-merge
  cancel-in-progress: false

jobs:
  verify-and-merge:
    if: ${{ !contains(github.event.head_commit.message, '[er22-scope-merge-gate]') }}
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    env:
      GH_TOKEN: ${{ github.token }}
      PR_NUMBER: "68"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: agent/ER-22-scope-depth-preflight-20260904
          fetch-depth: 0

      - name: Remove one-shot merge gate from the candidate tree
        shell: bash
        run: |
          set -euo pipefail
          state="$(gh pr view "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --json state --jq .state)"
          if [[ "$state" != "OPEN" ]]; then
            echo "PR is already $state"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm -- .github/workflows/er22-scope-verified-merge.yml
          git commit -m "ER-22: finalize scope preflight candidate [er22-scope-merge-gate]"
          git push origin HEAD:agent/ER-22-scope-depth-preflight-20260904
          echo "CANDIDATE_SHA=$(git rev-parse HEAD)" >> "$GITHUB_ENV"

      - name: Require the repository verify and rust checks on the exact candidate
        if: ${{ env.CANDIDATE_SHA != '' }}
        shell: bash
        run: |
          set -euo pipefail
          for _ in $(seq 1 75); do
            head_sha="$(gh pr view "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --json headRefOid --jq .headRefOid)"
            if [[ "$head_sha" != "$CANDIDATE_SHA" ]]; then
              echo "PR head moved from $CANDIDATE_SHA to $head_sha" >&2
              exit 1
            fi
            checks="$(gh api --paginate "/repos/$GITHUB_REPOSITORY/commits/$CANDIDATE_SHA/check-runs?per_page=100")"
            verify="$(jq -r '[.check_runs[] | select(.name == "verify")][-1].conclusion // "PENDING"' <<<"$checks")"
            rust="$(jq -r '[.check_runs[] | select(.name == "rust")][-1].conclusion // "PENDING"' <<<"$checks")"
            if [[ "$verify" == "success" && "$rust" == "success" ]]; then
              exit 0
            fi
            for conclusion in "$verify" "$rust"; do
              case "$conclusion" in
                failure|cancelled|timed_out|action_required|startup_failure|stale)
                  echo "required check failed: verify=$verify rust=$rust" >&2
                  exit 1
                  ;;
              esac
            done
            sleep 8
          done
          echo "required checks did not complete for $CANDIDATE_SHA" >&2
          exit 1

      - name: Squash-merge the exact green head
        if: ${{ env.CANDIDATE_SHA != '' }}
        shell: bash
        run: |
          set -euo pipefail
          gh pr merge "$PR_NUMBER" \
            --repo "$GITHUB_REPOSITORY" \
            --squash \
            --delete-branch \
            --match-head-commit "$CANDIDATE_SHA" \
            --subject "ER-22: reject excessive federation scope depth before recursive parsing (#68)" \
            --body "Preflight recognized federation scope trees iteratively before recursive Zod parsing, preserving the existing depth-32 contract and preventing stack exhaustion. Exact candidate passed required verify and rust checks."
