name: export-implementation-source
on:
  push:
    branches: [main]
    paths: [.github/workflows/export-implementation-source.yml]
permissions:
  contents: read
jobs:
  export:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: main
          fetch-depth: 0
          persist-credentials: false
      - name: Export the exact source commit without executing application code
        shell: bash
        run: |
          set -euo pipefail
          test "$(git branch --show-current)" = main
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          mkdir -p /tmp/eliotr-source
          git bundle create /tmp/eliotr-source/main.bundle main
          git rev-parse HEAD > /tmp/eliotr-source/SOURCE_SHA
          (cd /tmp/eliotr-source && sha256sum main.bundle > SHA256SUMS)
      - uses: actions/upload-artifact@v4
        with:
          name: run-controls-source
          path: /tmp/eliotr-source/
          retention-days: 1
          if-no-files-found: error
