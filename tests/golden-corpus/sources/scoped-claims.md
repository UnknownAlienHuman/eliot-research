# Scoped claims fixture

For corpus generation alpha, source family A reported no failed imports after twelve admitted PDF revisions.

Source family B reported two failed imports among five HTML captures because both responses were login stubs.

Therefore the statement “no imports failed” is true only for family A and false for the combined corpus.
