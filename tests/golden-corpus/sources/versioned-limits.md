# Versioned limits fixture

The superseded draft set the ordinary JSON request target at 128 KiB.

The active generation sets the ordinary JSON request hard limit at 256 KiB and the Drive contribution hard limit at 128 KiB.

These are internal release limits, not proof of Cloudflare platform ceilings.
